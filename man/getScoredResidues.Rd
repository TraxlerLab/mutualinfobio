% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-Alignment.R
\name{getScoredResidues}
\alias{getScoredResidues}
\title{Get Scored Residues for a Sequence}
\usage{
getScoredResidues(object, name)

getScoredResidues(object, name)
}
\arguments{
\item{object}{An \code{Alignment} object.}

\item{name}{The sequence name to extract.}
}
\value{
A data frame with columns: residue_index, alignment_position, residue, score

A data frame with columns: residue_index, alignment_position, residue, score
}
\description{
Given a sequence name, return a list of tuples:
(residue_index, alignment_position, residue, score)
Skips gaps and uses 0.0 if no MI score is available.

For a given sequence name, return a data frame with:
- residue_index (non-gap count)
- alignment_position (with gaps)
- residue (amino acid)
- score (from getTopMIFor; defaults to 0.0 if not found)
}
